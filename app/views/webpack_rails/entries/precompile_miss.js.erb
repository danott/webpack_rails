console.error(
  "Missing precompiled webpack entry '<%= entry_name %>'.\n\
  Expected to find '<%= entry_name %>' precompiled at '(Rails.root)/public/webpack/<%= entry_name %>', but it wasn't there."
);
